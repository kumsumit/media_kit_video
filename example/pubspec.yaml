name: media_kit_video_example
publish_to: "none"

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  media_kit_video:
    path: ../

  cupertino_icons: ^1.0.9

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
